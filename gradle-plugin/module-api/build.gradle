plugins {
    id 'java-library'
}

dependencies {
    // java-library 플러그인이 제공하는 기능으로 해당 모듈이 의존하는 라이브러리의 정보가 외부로 노출된다.
    api 'org.apache.httpcomponents:httpclient:4.5.7'
    // api와 다르게 module private 으로 외부에서 해당 라이브러리에 대한 접근이 불가능
    implementation 'org.apache.commons:commons-lang3:3.5'
}
